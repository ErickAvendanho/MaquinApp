import 'package:flutter/material.dart';

class JobDetailFull extends StatelessWidget {
  const JobDetailFull({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Página con detalles completa')
      ),
    );
  }
}
